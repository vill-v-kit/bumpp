import "../Builtins.pkl"
import "../Config.pkl"

@Builtins.meta {
  category = "Ruby"
  description = "ERB template linter"
}
erb = new Config.Step {
  glob = "**/*.erb"
  check = new Config.CommandSpec {
    command = "erb -P -x -T - {{ files }} | ruby -c"
    effect = "read"
  }
}
